# Python cache and compiled files
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environments
.venv/
venv/
ENV/
env/

# Logs (keep them out of Git, they grow too fast)
*.log
logs/

# CSV (we don’t use them anymore)
*.csv

# JSON reports (we DO want to keep these)
# so don’t ignore *.json

# Reports (keep reports/ in Git so JSON files are versioned)
# reports/

# Backups (we’ll decide later if we want to keep them, so ignore for now)
backups/

# Config and secrets
.env
config/*.yaml

# IDE / Editor junk
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

